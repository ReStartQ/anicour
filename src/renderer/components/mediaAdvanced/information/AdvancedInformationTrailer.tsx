import { Box } from '@mui/material';
import React from 'react';
import TrailerButton from '../extra/TrailerButton';

const AdvancedInformationTrailer = () => {
  return (
    <Box>
      <TrailerButton />
    </Box>
  );
};

export default AdvancedInformationTrailer;
